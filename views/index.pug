extends layout

block content
  h1 Express 
  p Welcome to Express
  p Hello This is Aditya SADHU 
    title Express 
    link(rel='stylesheet', href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css')
  body
    p Welcome to My App
    p(id='boost') Boosted values will appear here.

    button(type="button", class="btn btn-secondary", onclick="boost()") Sample Button

    script.
      let value = 0;
      let amt = 3;

      function boost() {
        amt += 1; 
        value += amt;
        document.getElementById('boost').textContent = Boosted ${value} by ${amt};
      }