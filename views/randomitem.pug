extends layout

block content
  h1= title
  p Welcome to Aditya's #{title}

  title Express 
  link(rel='stylesheet', href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css')

  .container
    .row
      each image, index in randomImages
        .col-sm-4
          img(src=/images/${image}, alt='Random Toy', height='300', width='300')